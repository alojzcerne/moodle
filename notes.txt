
cd /home/lojz/md/projekti/CDIUL/moodle

( mkdir /tmp/moodle_$$; cd /tmp/moodle_$$; 
  git clone git://git.moodle.org/moodle.git;
  ( cd moodle; 
    git checkout MOODLE_404_STABLE;
    git pull origin MOODLE_404_STABLE;
    git archive --format=tar.gz -o /home/lojz/md/projekti/CDIUL/moodle/moodle.tar.gz MOODLE_404_STABLE
  ); 
  rm -rf /tmp/moodle_$$;
)

ls -la /home/lojz/.local/share/containers/storage/volumes/cdiul_moodle_data/_data/

podman login -u alojzcerne quay.io
( cd /home/lojz/md/projekti/CDIUL/moodle; 
  podman build -t moodle .
  podman tag moodle quay.io/alojzcerne/moodle:latest; podman push quay.io/alojzcerne/moodle:latest
)
oc import-image mdleapp 

podman run --rm -it moodle bash

podman-compose down; podman volume rm cdiul_mariadb_data cdiul_moodle_data; podman-compose up -d
podman-compose down; podman-compose up -d
podman exec -it cdiul_moodle_1 bash



oc login -u admin https://api.ocp4.martinj.hrib.net:6443
oc new-project mdl02

oc process -f /home/lojz/md/projekti/CDIUL/resources/moodle.yaml \
  -p HOST=mdl02.apps.ocp4.martinj.hrib.net \
  -p TEMPLATE_ID=01 \
  -p FULL_NAME=Razred \
  -p SHORT_NAME=RDR \
  | oc apply -f -
